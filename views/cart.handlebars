<div class="cart">
    <div class="list">
        {{#each products}}
        <div class="product">
            <p>{{name}}</p>
            <button>Click</button>
        </div>
        {{/each}}
    </div>
    <script src="/scripts/cart.js"></script>
</div>